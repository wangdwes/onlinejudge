
public class Solution {
	public boolean isPalindrome(String s) {
		s = s.toLowerCase();
		
		String filtered; 
		for (int index = 0; index < s.length(); index++) {
			if (s.charAt(index) >= 'A' && s.charAt(index) <= 'Z') filter +=
		}
	}
}
